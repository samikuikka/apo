{
  "name": "apo-agent-task-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bundled demo task tree for the demo workspace. Declares the runtime deps the demo adapters import so the backend's task-dependency installer can resolve them in the container (where the monorepo's node_modules is not present).",
  "dependencies": {
    "zod": "^4.0.0",
    "ai": "^6.0.3",
    "@ai-sdk/openai": "^3.0.1",
    "@anthropic-ai/claude-agent-sdk": "^0.3.212",
    "@anthropic-ai/sdk": "^0.110.0"
  }
}
