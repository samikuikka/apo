---
title: "apo runs deliverable"
description: "Read a run's deliverables, a manifest list, or one deliverable's full content."
---

import TerminalOutput from '../../../components/demos/TerminalOutput.astro';

`apo runs deliverable` reads a run's deliverables without re-rendering the whole run. It's the counterpart to [`apo runs show`](/cli/runs-show/): `runs show` keeps large per-check values as a one-line manifest so a failed run stays readable, and this command is how you fetch the actual content when you need it.

## Synopsis

```bash
apo runs deliverable <run-id> [name] [--output <path>]
```

## Description

The first argument is a **run id**, a unique prefix, or `last` (the most recent run). The optional second argument is a **deliverable name**.

- **No name**: print a manifest of every deliverable: its name, kind (`json` or `artifact`), and size in bytes. Nothing large is dumped.
- **With a name**: fetch that deliverable's content. JSON deliverables print as pretty JSON; binary **artifacts** stream to a file via `--output` (dumping binary bytes to an interactive TTY is refused).

## Arguments

| Argument | Required | Purpose |
|---|---|---|
| `<run-id>` | yes | Run id, unique prefix, or `last`. |
| `[name]` | no | Deliverable name. Omit to list all deliverables as a manifest. |

## Options

| Option | Default | Purpose |
|---|---|---|
| `--task <id>` | - | When using `last`, scope to the latest run of this task. |
| `--output <path>` | - | Write a binary artifact to a file. Use `.` or a trailing `/` dir to auto-derive the filename from the deliverable's original name. |

Supports the global `--json` flag.

## Example

List every deliverable as a manifest:

```bash
apo runs deliverable run_de89cab0f1e2d3a4b5c6d7e8
```

<TerminalOutput
  lines={[
    { text: "Deliverables for run run_de89cab0f1e2d3a4b5c6d7e8:", tone: "bold" },
    { text: "  memorandum  json      20,000 bytes" },
    { text: "  summary     json         342 bytes" },
    { text: "  report      artifact  18,432 bytes (invoice-report.pdf)" },
    { text: "" },
    { text: "Read one: apo runs deliverable <run-id> <name>", tone: "dim" },
  ]}
/>

Read one JSON deliverable's full content:

```bash
apo runs deliverable run_de89cab0f1e2d3a4b5c6d7e8 memorandum
```

Download a binary artifact to a file:

```bash
apo runs deliverable run_de89cab0f1e2d3a4b5c6d7e8 report --output .
# writes invoice-report.pdf in the current directory
```

Machine-readable manifest for scripting:

```bash
apo runs deliverable run_de89cab0f1e2d3a4b5c6d7e8 --json
```

```json
[
  {
    "id": "dlv_01",
    "name": "memorandum",
    "kind": "json",
    "status": "ready",
    "media_type": "application/json",
    "display_filename": null,
    "size_bytes": 20000,
    "sha256": "a1b2…",
    "download_url": null
  },
  {
    "id": "dlv_02",
    "name": "report",
    "kind": "artifact",
    "status": "ready",
    "media_type": "application/pdf",
    "display_filename": "invoice-report.pdf",
    "size_bytes": 18432,
    "sha256": "c3d4…",
    "download_url": null
  }
]
```

## Exit codes

| Code | Meaning |
|---|---|
| `0` | Manifest printed, or the named deliverable's content printed. Also `0` when the run simply has no deliverables. |
| `2` | Run not found, connection failure, or the named deliverable doesn't exist (available names are listed). |

## See also

- [`apo runs show`](/cli/runs-show/): the run verdict and per-test breakdown (deliverable values shown as manifests by default).
- [`apo task run`](/cli/task-run/): produce a run whose deliverables you want to read.
