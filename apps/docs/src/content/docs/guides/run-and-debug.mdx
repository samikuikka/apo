---
title: Run and debug
description: Run a task, read the result, debug the failure through the trace.
---

import TerminalDemo from '../../../components/demos/TerminalDemo.astro';

**This is the main loop.** Run a task, see whether it passed, and when it fails, find out why. Every run, whether manual, batch, or scheduled, flows through the same steps.

## 1. Run a task

<TerminalDemo
	command="apo task run extract-parties"
	lines={[
		{ text: "Executor: caller (recorded in a1b2c3d4)", tone: "dim" },
		{ text: "Revision: clean worktree 9f2c7d81e4b9 from github.com/acme/agent", tone: "dim" },
		{ text: "" },
		{ parts: [{ text: "FAIL", tone: "danger" }, { text: " extract-parties", tone: "bold" }] },
		{ parts: [{ text: "  Checks:", tone: "bold" }] },
		{ parts: [{ text: "    " }, { text: "FAIL", tone: "danger" }, { text: " parties-are-complete" }] },
		{ text: "" },
		{ parts: [{ text: "Run:     ", tone: "dim" }, { text: "run_de89cab0f1e2d3a4b5c6d7e8", tone: "bold" }] },
		{ parts: [{ text: "Inspect: ", tone: "dim" }, { text: "apo runs show run_de89cab0f1e2d3a4b5c6d7e8", tone: "dim" }] },
	]}
/>

## 2. Read the breakdown

The result is pass or fail, but the breakdown shows which test broke and how:

<TerminalDemo
	command="apo runs show"
	lines={[
		{ text: "(latest run)", tone: "dim" },
		{ parts: [{ text: "Run: run_de89cab0f1e2d3a4b5c6d7e8", tone: "bold" }] },
		{ text: "  Task:     extract-parties" },
		{ text: "  Status:   failed" },
		{ parts: [{ text: "  Result:   " }, { text: "FAIL", tone: "danger" }] },
		{ text: "  Checks:   1/2 passed (1 failed)" },
		{ text: "  Cost:     $0.004200" },
		{ text: "" },
		{ parts: [{ text: "  Checks:", tone: "bold" }] },
		{ parts: [{ text: "    " }, { text: "PASS", tone: "success" }, { text: " used-source-document" }] },
		{ parts: [{ text: "    " }, { text: "FAIL", tone: "danger" }, { text: " parties-are-complete" }] },
		{ parts: [{ text: "      " }, { text: "✗", tone: "danger" }, { text: " check" }] },
		{ parts: [{ text: "        extract-parties.eval.ts:31:5", tone: "dim" }] },
		{ parts: [{ text: "        " }, { text: "− Expected: ", tone: "success" }, { text: "every named party captured" }] },
		{ parts: [{ text: "        " }, { text: "+ Received: ", tone: "danger" }, { text: 'missing "acme-corp"' }] },
		{ text: "" },
		{ parts: [{ text: "  Trace:    " }, { text: "f1a3e9c0d5e6f7a8b9c0d1e2f3a4b5c6", tone: "bold" }, { text: "  (apo traces show f1a3e9c0…)", tone: "dim" }] },
	]}
/>

Start here. The failing test tells you *what* broke: `parties-are-complete` failed because "acme-corp" was missing. That's your hypothesis. The trace tells you *why*.

## 3. Open the trace

<TerminalDemo
	command="apo traces show f1a3e9c0"
	lines={[
		{ parts: [{ text: "Trace: f1a3e9c0d5e6f7a8b9c0d1e2f3a4b5c6", tone: "bold" }] },
		{ text: "  Task:      extract-parties" },
		{ text: "  Status:    success" },
		{ text: "  Duration:  4.2s" },
		{ text: "  Cost:      $0.004200" },
		{ text: "  Tokens:    3,841" },
		{ text: "" },
		{ parts: [{ text: "  Calls:", tone: "bold" }] },
		{ text: "    read_file                0.3s     412 tok" },
		{ text: '        args: {"path":"contract.pdf"}' },
		{ text: "    extract_entities         1.1s   1,204 tok" },
		{ text: "    generate_response        2.4s   2,225 tok" },
		{ text: "        tokens: 1,890 prompt + 335 completion" },
	]}
/>

The trace is the full runtime record of the run: the call tree, the token counts, the actual messages exchanged. Walk it to confirm or refute the hypothesis from the failing test.

What to look for:

- **The call tree**: which tools the agent called, in what order, with what input. Did it call `read_file`? Did it call `extract_entities`? Where did the behavior diverge from what the test expected?
- **The messages**: the actual system, user, and assistant messages. This is where "why did it do that" gets answered. You can read exactly what the agent was told and what it said back.
- **Token usage and params**: per call. A failure that looks like a reasoning bug is sometimes a model or temperature mismatch.

## 4. Fix and re-run

Once you've found the cause, the fix depends on **who was wrong**: inspect the evidence first, then branch:

- **The agent was wrong** (the trace shows it skipped the document, the deliverable misses the substance): fix the agent (prompt, tools, model) and re-run. The new run gets its own trace, and you can compare against the previous failure to confirm the fix landed.
- **The test was wrong** (you can demonstrate it against the recorded evidence, the substance is present, the judge read a parenthetical as a requirement): correct the history, then repair the test:

  ```bash
  # 1. Correct this run's recorded result (evidence stays untouched)
  apo runs correct run_de89cab "report-is-complete" --pass \
    --reason "Retention is present in the KPI table; judge missed it"

  # 2. Repair the test itself so the NEXT run doesn't need the correction
  ```

  Correcting history doesn't fix the future, the next run fails the same way until the instruction or matcher is repaired. See [Manual corrections](/concepts/tests/#manual-corrections).

The `Trace:` id in the breakdown is that run's one trace, there's never ambiguity about which trace belongs to a run. See [Mental model](/concepts/mental-model/) for the one-trace-per-run rule.

## Batches

A batch run is one execution that may produce several task runs. Batches are how selection-driven and scheduled runs work: you define a subset of tasks and run them together. A batch provides shared context (the selection, the trigger, the timing) across its task runs, but each task run still gets its own result, breakdown, and trace.

<TerminalDemo
	command="apo batch show bch_7c0a"
	lines={[
		{ parts: [{ text: "Batch: bch_7c0a89f9525e6735b9cb5b1b", tone: "bold" }] },
		{ text: "  Status:   completed" },
		{ text: "  Tasks:    3" },
		{ text: "  Passed:   2" },
		{ text: "  Failed:   1" },
		{ text: "  Errored:  0" },
		{ text: "  Cost:     $0.015300" },
	]}
/>

Use `apo batch show <batch-id>` to see the batch's task runs and outcomes.
