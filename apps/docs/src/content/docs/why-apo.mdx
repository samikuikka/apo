---
title: Why apo
description: Why agent systems need executable definitions of done.
---

import GradedAcrossRunsDemo from '../../components/demos/GradedAcrossRunsDemo.astro';
import DeliverableDemo from '../../components/demos/DeliverableDemo.astro';
import SchedulesDemo from '../../components/demos/SchedulesDemo.astro';

Harness development is a feedback loop. Someone changes the harness, runs the agent, inspects the outcome, decides whether it is good enough, and tries again. **apo makes that loop operable by humans and coding agents without surrendering the standard to either one.**

```text
expert knowledge → executable specification → real harness run
                                              ↓
                              PASS / FAIL + useful evidence
                                              ↓
                                  improve the harness ↺
```

Your experts define the real jobs, conditions, outcomes, and requirements that constitute done. apo turns that judgment into executable tests, runs the real agent system, and returns a verdict with the test breakdown, trace, and deliverables. If evaluation cannot complete, the Task Run surfaces an error instead.

The specification does more than prevent regressions. It is the interface between expert knowledge and whoever improves the system. A coding agent can use pass/fail as a control signal, read the failed tests, trace, and deliverables, change the implementation, and run again without asking an expert to judge every attempt.

apo owns the Task Run and evidence. It does not edit the implementation or autonomously start another Task Run. It supplies the trusted feedback environment that makes the surrounding practice possible. That practice is **harness engineering**.

## Two outcomes, one specification

The same primitives serve the organization and the people building the harness.

| | What the specification provides |
|---|---|
| **The organization** | An executable record of what the agent system is required to do, and evidence of whether that standard still holds as models, features, and harnesses change. |
| **Domain experts** | A place to encode and strengthen their judgment instead of manually reviewing every attempt. |
| **Harness engineers** | A stable target for changes to prompts, tools, context, orchestration, models, memory, retries, and code. |
| **Coding agents** | A machine-usable definition of done, a way to run the real system, and actionable evidence when it fails. |

A specification without an engineering loop is a test suite. A loop without an authoritative specification is aimless automation. apo connects expert intent to real execution, independent judgment, useful failure evidence, and the next engineering action.

## Three starting points

The agent tooling ecosystem grew from several directions, and most of it sorts into two groups with established winners.

**Testing and eval frameworks** (DeepEval, Promptfoo, and their neighbors) start from a test case plus an evaluator. Their question: *did this execution satisfy the assertion or metric I defined?*

**Observability and eval platforms** (Arize, Langfuse, Braintrust) start from an execution record: capture traces of what your AI already did, score them, mine datasets from them, monitor what ships. Their question: *how is my system behaving, and did this change make it better?*

apo starts from a different object: the **capability specification**.

*What must this harness be capable of doing, under these conditions, within these constraints, leaving behind this result?*

apo makes that statement executable: it loads the task with its fixtures, runs the real harness, collects what the agent produced, and returns a verdict.

| | Primary question |
|---|---|
| Testing & eval frameworks | Did this execution satisfy my test? |
| Observability & eval platforms | How is my AI system behaving and changing? |
| **apo** | **Can this harness do what we require?** |

There is overlap between all three, and that's fine. You can assemble this workflow from testing libraries, observability platforms, artifact parsers, CI scripts, and instructions for coding agents. Then your team owns the glue: how a real job becomes a testable outcome, how tests become one verdict, how that verdict links to the deliverables and trace, and how a coding agent navigates the repair loop.

apo doesn't replace an observability stack: production failures found there become apo regression tests, and existing evaluators can help verify a specification. The distinction is what everything is organized around. In apo, the primary artifact is the executable definition of what your agent system must be capable of doing, and every other primitive serves the loop that engineers the harness against it.

## The specification is the source of truth

Platform pipelines run one way: production traffic → traces → datasets mined from them → experiments → monitoring. Behavior first; standards derived from it.

apo runs the other way:

```text
capability specification → run the real harness → PASS / FAIL
   ↑                                              │
   └──────── improve the harness until it passes ─┘
```

Production traces can tell you what happened. They cannot define what your system should be. A specification can. It applies an explicit standard every run, gates changes, and gives a coding agent a concrete target to work against. When a run reveals an unacceptable behavior nobody anticipated, the team strengthens the specification; the discovery becomes part of the standard instead of remaining in somebody's memory.

Most of the choices below fall out of taking that specification seriously.

## Run the real thing

**The only test worth running is the one that runs the real thing.**

An agent is not a prompt. It's a prompt plus tools plus the code that wires them together. Scoring the prompt in isolation tells you about the prompt. It tells you nothing about whether the agent actually works.

So apo runs your real agent orchestration through the adapter and judges what comes back. The task may use safe test data, sandboxed tools, or test dependencies; what matters is that you exercise the implementation as a system instead of scoring a prompt-only proxy. If you change the orchestration, the next run reflects it. If you introduce a behavioral regression, the tests can catch it.

## Judge the task outcome, not the self-report

**What the agent actually accomplished matters more than what it claims.**

For many agent systems, the conversation is not the result. The result is an artifact, a file written, a state changed, a report generated, an action taken, or another outcome the agent was asked to produce. When the conversation itself is the job, it can be the deliverable too.

apo is built around those deliverables. A task declares what the agent should produce, the adapter collects it after the run, and the tests assert on the actual output, not on whether the conversation *sounded* right. A response that's polite, fluent, and completely wrong should fail. A response that's terse but produced the right artifact should pass.

<DeliverableDemo />

This is why the adapter exists, and why it's your responsibility to write: only your code knows what "the deliverable" even *is* for your agent. apo can't guess it, and shouldn't.

## One file, three concerns

**The task, the input, and the tests live in one `.eval.ts` file.**

Most eval setups scatter these across config files, prompt templates, fixture directories, and a separate assertions module, so understanding a single evaluation means opening five tabs. apo refuses that. A task is one folder with one file: `task(...)` registers the definition, `turn(...)` decides what the agent sees, and the tests sit right there next to them. Read the file top to bottom and you've read the whole evaluation.

This isn't an aesthetic preference. When a run fails, the breakdown points at a test by name, and that test lives in the same file as everything that produced the failure. The cause and its context are one scroll apart, not linked across a project tree.

## One kind of test

**A test is a test. Some run in code, some ask an LLM. Both are the same shape.**

Some tests are exact: did the agent call `read_file` on the contract? Does the output match the schema? Did anything error?

Others need judgment: did the response actually meet what a user would expect? That's not a `===` question, and pretending it is leaves the most important checks unwritten.

Both are written the same way: a `test(...)` block.

```typescript
// deterministic: runs in code, no model
test("used-source-document", (t) => {
  t.calledTool("read_file", { input: { path: "contract.pdf" } });
  t.noFailedActions();
});

// judged: hands the deliverable to an LLM
test("parties-are-complete", async (t, { deliverables }) => {
  t.check(deliverables.parties, matches(partiesSchema));
  await t.judge(
    deliverables.parties,
    "PASS when every named party is captured without false positives.",
  );
});
```

We didn't want two systems, with "checks" over here and "evals" over there, because the moment you split them, people reach for the wrong one. They'll write a judge where a five-line assertion would do, or skip judgment entirely because standing up a separate eval is too much friction.

## Binary results

**A completed evaluation passes or fails. There is no score.**

`0.78` doesn't tell you whether to ship. It doesn't tell you whether the run met the standard. And it lets a near-pass quietly feel like a win, which is the most dangerous failure mode in evaluation, the one you don't investigate.

The question for a completed evaluation is binary: did it meet the bar, or didn't it? If any test fails, the run fails. No weighting, no averaging, no partial credit. If evaluation cannot complete, the Task Run reports an error rather than pretending it received a verdict.

## Graded across runs

**Binary is right for a decision. It's blind to a trajectory.**

Here's the problem. A run that fails 1 of 15 tests and a run that fails 14 of 15 are both "fail." Collapse them to a 0% pass-rate and you've thrown away the single most useful signal in evaluation: *did the last change help?*

So apo keeps the per-test counts. Each evaluated run is still pass or fail; the result doesn't soften, but across runs you can read the direction. `14/15 → 9/15` is a regression even though both are red. `2/15 → 11/15` is real progress even though neither ships yet.

<GradedAcrossRunsDemo />

:::note
The graded signal is for *comparing* runs. It does not turn the result into a score. A 14/15 run is still a fail.
:::

## Failures need traces

**When a run fails, you should be able to see exactly what the agent did.**

The breakdown tells you *what* failed: which test, what was expected, what came back. The trace tells you *why*: the full call tree, every tool invocation with its input and output, the token counts, and the actual messages exchanged between the agent and the model.

apo treats the trace as a first-class surface, not an afterthought bolted onto an eval tool. Each run owns exactly one trace: never two competing ones, never a guessing game about which trace belongs to which run. A failure you can't debug is a failure you can't fix.

But the trace is evidence, not the workflow. It exists because a test failed, pytest can print an enormous stack trace, and nobody calls pytest a stack-trace analytics product. When a run passes, you may never open the trace at all.

## Schedules as policy

**How often a task runs is a policy decision, not a property of the task.**

A task says *what* to evaluate. When it runs is a separate concern, and the right cadence depends on the subset: smoke tests are cheap and should run on every commit; the full suite is expensive and belongs nightly; regression suites run when something is about to ship. So apo keeps cadence out of the task and puts it in a schedule, where different subsets can run at different rates instead of one timer running everything all the time.

<SchedulesDemo />

## Make the loop agent-operable

**A coding agent can operate the implementation loop itself.** Your experts encode their judgment in the specification, then the coding agent runs the task from the CLI, reads the result, opens the relevant evidence, iterates on the codebase, and runs again until the tests pass. Your expertise lives in the standard; the agent applies it without asking you to judge each attempt.

The thing being engineered is not a prompt, but the harness itself: model choice, prompts, context construction, tools and their descriptions, orchestration, memory, retries, and code. The test suite is the interface between your domain expertise and the agents improving the system.

apo is the testing and feedback system inside that loop, not the repair agent. It runs one Task Run at a time and reports what happened. The coding agent owns the edits and decides when to start another Task Run.

See [Close the loop with a coding agent](/guides/loop-engineering/) for the workflow.

---

If that matches how you think about agent testing, the [Quickstart](/quickstart/) will get you to a result.
